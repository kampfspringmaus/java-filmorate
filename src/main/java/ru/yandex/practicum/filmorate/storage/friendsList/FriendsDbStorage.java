package ru.yandex.practicum.filmorate.storage.friendsList;

import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;
import ru.yandex.practicum.filmorate.model.FriendsList;
import ru.yandex.practicum.filmorate.model.User;

import java.util.Map;
import java.util.Optional;

public class FriendsDbStorage {
  /*  protected final JdbcTemplate jdbc;
    protected final RowMapper<FriendsList> mapper;

    private static final String FIND_ALL_QUERY = "select ";
    protected Optional<Map<Integer, String>> findAll(String query, Object... params) {
        try {
            Map<Integer, String> result = jdbc.queryForObject(query, mapper, params);


        }
    }*/
}
